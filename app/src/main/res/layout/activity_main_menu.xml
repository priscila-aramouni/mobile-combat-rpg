<?xml version="1.0" encoding="utf-8"?>

<!--
This app targets Android API 28 (can be run on API >= 26).
It was built for a resolution of 1080 × 1920 (Nexus 5X).
-->

<RelativeLayout
    android:id="@+id/root_layout_main"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/main_menu_background">
    android:alpha="1"
    tools:context=".MainActivity">

        <Button
            android:layout_width="120dp"
            android:layout_height="30dp"
            android:text="Play"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:background = "@drawable/button_selector"
            android:onClick="goToHeroSelection" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="20dp"
        android:orientation="vertical">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Total wins: "
        android:textColor="@color/owOrange"
        android:textSize="12sp"/>

    <TextView
        android:id="@+id/player_wins"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Player 1: 10 wins\nPlayer 2: 6 wins"
        android:textColor="@color/white"
        android:textSize="10sp"/>

    </LinearLayout>
</RelativeLayout>